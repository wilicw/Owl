import Text from '@/style-components/Text';
import Container from '@/style-components/Container';

function Waiting() {
  return (
    <Container style={{ justifyContent: 'center', height: '100%' }}>
      <Text block align="center">Waiting Data...</Text>
    </Container>
  );
}

export default Waiting;
